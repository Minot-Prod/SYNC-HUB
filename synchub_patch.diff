*** Begin Patch
*** Update File: data/opportunities.json
@@
-    "valueEUR": 3500,
+    // switched to CAD – adjust numeric values if conversion needed
+    "valueCAD": 3500,
@@
-    "valueEUR": 5800,
+    // switched to CAD – adjust numeric values if conversion needed
+    "valueCAD": 5800,
@@
-    "valueEUR": 12000,
+    // switched to CAD – adjust numeric values if conversion needed
+    "valueCAD": 12000,
@@
-    "valueEUR": 2100,
+    // switched to CAD – adjust numeric values if conversion needed
+    "valueCAD": 2100,
*** End Patch
*** Begin Patch
*** Update File: src/lib/storage/jsonStore.js
@@
-    valueEUR: 3500,
+    // switched to CAD – adjust numeric values if conversion needed
+    valueCAD: 3500,
@@
-    valueEUR: 5800,
+    // switched to CAD – adjust numeric values if conversion needed
+    valueCAD: 5800,
@@
-    valueEUR: 12000,
+    // switched to CAD – adjust numeric values if conversion needed
+    valueCAD: 12000,
@@
-    valueEUR: 2100,
+    // switched to CAD – adjust numeric values if conversion needed
+    valueCAD: 2100,
*** End Patch
*** Begin Patch
*** Update File: src/lib/services/opportunitiesService.js
@@ function getOpportunitiesStats() {
-  const totalPipelineValue = items
-    .filter((opp) => opp.stage !== 'won' && opp.stage !== 'lost')
-    .reduce((sum, opp) => sum + (opp.valueEUR || 0), 0);
+  const totalPipelineValue = items
+    .filter((opp) => opp.stage !== 'won' && opp.stage !== 'lost')
+    .reduce((sum, opp) => sum + (opp.valueCAD || 0), 0);
@@ function getOpportunitiesStats() {
-  const totalWonValue = items
-    .filter((opp) => opp.stage === 'won')
-    .reduce((sum, opp) => sum + (opp.valueEUR || 0), 0);
+  const totalWonValue = items
+    .filter((opp) => opp.stage === 'won')
+    .reduce((sum, opp) => sum + (opp.valueCAD || 0), 0);
*** End Patch
*** Begin Patch
*** Update File: src/lib/agents.js
@@ export const agents = {
   sales: {
     id: "sales",
     label: "Assistant commercial general",
     model: "gpt-4o-mini",
     systemPrompt:
       "Tu es un assistant commercial general pour Sync Productions. Tu aides a presenter l\\'offre, structurer les arguments de vente et repondre aux questions des prospects. Tu reponds en francais, de maniere claire, concise et oriente business.",
+    // chemin de l'avatar pour l'agent (sera remplacé plus tard par un lien distant)
+    avatar: "/avatars/sales.png",
   },
   prospection: {
@@
     model: "gpt-4o-mini",
     systemPrompt:
       "Tu es l\\'agent de prospection de Sync Productions. Ta mission est d\\'aider a identifier, qualifier et approcher de nouveaux prospects. Tu proposes des messages d\\'approche, sequences de relance et angles d\\'accroche adaptes. Style direct, professionnel, francais.",
+    avatar: "/avatars/prospection.png",
   },
   messages: {
@@
     model: "gpt-4o-mini",
     systemPrompt:
       "Tu es l\\'agent specialise dans les messages et scripts de Sync Productions. Tu aides a ecrire des mails, messages LinkedIn, scripts d\\'appels et reponses aux objections. Tu produis des textes concrets et pret a l\\'emploi, en francais.",
+    avatar: "/avatars/messages.png",
   },
   analyste: {
@@
     model: "gpt-4o-mini",
     systemPrompt:
       "Tu es l\\'analyste ventes de Sync Productions. Tu aides a analyser le contexte d\\'une entreprise, les besoins probables, les signaux forts/faibles et la pertinence de Sync Productions pour ce compte. Tu produis des analyses structurees et actionnables.",
+    avatar: "/avatars/analyste.png",
   },
   radar: {
@@
     model: "gpt-4o-mini",
     systemPrompt:
       "Tu es le radar d\\'opportunites de Sync Productions. A partir de listes de prospects, signaux ou notes, tu aides a prioriser ou a identifier les meilleures opportunites, avec un focus ROI/fit business.",
+    avatar: "/avatars/radar.png",
   },
*** End Patch
*** Begin Patch
*** Update File: src/pages/dashboard/index.js
@@ const hotDeals = [...safeOpportunities]
-    .map((o) => ({
-      ...o,
-      score: (o.valueEUR || 0) * (o.probability || 0),
-    }))
+    .map((o) => ({
+      ...o,
+      // use CAD value for scoring
+      score: (o.valueCAD || 0) * (o.probability || 0),
+    }))
@@ function DashboardPage(props) {
            <div style={layoutStyles.card}>
@@
-            <p style={layoutStyles.cardNumberMain}>
-              {totalPipelineValue.toLocaleString("fr-FR")} €
-            </p>
+            <p style={layoutStyles.cardNumberMain}>
+              {totalPipelineValue.toLocaleString("en-CA", { style: "currency", currency: "CAD" })}
+            </p>
@@
-            <p style={layoutStyles.cardNumberMain}>
-              {totalWonValue.toLocaleString("fr-FR")} €
-            </p>
+            <p style={layoutStyles.cardNumberMain}>
+              {totalWonValue.toLocaleString("en-CA", { style: "currency", currency: "CAD" })}
+            </p>
@@
-                     {opp.client} — {opp.valueEUR.toLocaleString("fr-FR")} €
+                     {opp.client} — {opp.valueCAD.toLocaleString("en-CA", { style: "currency", currency: "CAD" })}
@@
-                     {opp.valueEUR.toLocaleString("fr-FR")} €
+                     {opp.valueCAD.toLocaleString("en-CA", { style: "currency", currency: "CAD" })}
*** End Patch
*** Begin Patch
*** Update File: src/pages/opportunities/index.js
@@
-                     {opp.valueEUR.toLocaleString("fr-FR")} €
+                     {opp.valueCAD.toLocaleString("en-CA", { style: "currency", currency: "CAD" })}
*** End Patch
*** Begin Patch
*** Add File: src/pages/agents/index.js
+import React from "react";
+import ShellLayout from "../../components/ShellLayout";
+import { agents } from "../../lib/agents";
+
+const styles = {
+  main: {
+    padding: "24px",
+    width: "100%",
+    height: "100%",
+    overflowY: "auto",
+    display: "flex",
+    flexDirection: "column",
+    gap: "24px",
+    color: "white",
+  },
+  grid: {
+    display: "grid",
+    gridTemplateColumns: "repeat(auto-fit, minmax(220px, 1fr))",
+    gap: "20px",
+  },
+  card: {
+    borderRadius: "16px",
+    background: "rgba(15, 23, 42, 0.8)",
+    border: "1px solid rgba(148, 163, 184, 0.4)",
+    boxShadow: "0 18px 45px rgba(15, 23, 42, 0.7)",
+    padding: "16px",
+    display: "flex",
+    flexDirection: "column",
+    alignItems: "center",
+    textAlign: "center",
+    gap: "8px",
+  },
+  avatar: {
+    width: "80px",
+    height: "80px",
+    borderRadius: "50%",
+    objectFit: "cover",
+    marginBottom: "8px",
+  },
+  label: {
+    fontSize: "14px",
+    fontWeight: 600,
+  },
+};
+
+export default function AgentsPage() {
+  return (
+    <ShellLayout>
+      <main style={styles.main}>
+        <header>
+          <h1 style={{ fontSize: "26px", fontWeight: 700 }}>
+            Nos agents
+          </h1>
+          <p style={{ color: "#cbd5f5", marginTop: "4px" }}>
+            Choisissez l'agent adapte a votre besoin. Chaque agent dispose d'un role precis.
+          </p>
+        </header>
+        <section style={styles.grid}>
+          {Object.values(agents).map((agent) => (
+            <div key={agent.id} style={styles.card}>
+              {agent.avatar && (
+                <img
+                  src={agent.avatar}
+                  alt={agent.label}
+                  style={styles.avatar}
+                />
+              )}
+              <div style={styles.label}>{agent.label}</div>
+            </div>
+          ))}
+        </section>
+      </main>
+    </ShellLayout>
+  );
+}
*** End Patch
*** Begin Patch
*** Update File: src/components/ShellLayout.module.css
@@ :root {
-  --accent-1:#4f8cff;
-  --accent-2:#7b5cff;
+  /* Parlios palette */
+  --accent-1:#c084fc;
+  --accent-2:#f472b6;
*** End Patch
*** Begin Patch
*** Update File: src/components/HeaderBar.module.css
@@
-.cta { padding:8px 14px; border-radius:999px; border:none; background: linear-gradient(135deg, #00f0ff, #3b82f6, #8b5cff); color:#020617; font-weight:700; font-size:13px; cursor:pointer;}
+.cta { padding:8px 14px; border-radius:999px; border:none; background: linear-gradient(135deg, #c084fc, #f472b6); color:#020617; font-weight:700; font-size:13px; cursor:pointer;}
*** End Patch
*** End Patch
